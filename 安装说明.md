# DPCC-OCS-AI 安装说明

一个在**任意网页**上用 AI 帮你答题的油猴脚本：支持「拖拽框选 → 真实截图 → 多模态 AI 解答」，也支持读取 DOM 识别题目。

## 一、安装（普通用户）

1. 先给浏览器装 **Tampermonkey（油猴）** 扩展：<https://www.tampermonkey.net/>
   （Scriptcat 脚本猫也可以；必须支持 `GM_xmlhttpRequest`）
2. 点下面这个链接，油猴会自动弹出安装界面，点「安装」即可：

   👉 **<https://raw.githubusercontent.com/DUNHKpcc/ocs-ai-/ai-answer-assistant/ocs.ai.user.js>**

3. 安装后脚本已内置自动更新（`@updateURL`），仓库更新后油猴会自动提示升级。

> 如果点链接没反应：复制上面的链接，在油猴管理面板「实用工具 → 从 URL 导入」里粘贴导入。

## 二、配置（首次使用必做）

打开任意正常网站（如 `https://www.baidu.com`），页面上会出现 OCS 悬浮窗，点开「🤖 AI答题助手」，填写：

- **Base URL**：你的 OpenAI 兼容接口地址（如 `https://api.openai.com/v1` 或自建/中转地址）
- **API Key**：你的密钥
- **模型**：如 `gpt-4o-mini`、`gpt-4o` 等**支持图片（多模态）**的模型
- 图片发送方式：截图模式会自动用多模态，保持默认即可

## 三、用法

### 截图搜题（推荐）
1. 点「**拖拽框选截图**」，在页面上拖一个框选住题目；
2. 浏览器弹出共享窗口时选「**此标签页**」并允许（一次会话只需授权一次）；
3. AI 看图作答，答案和**截图缩略图**显示在面板里。
4. 同一区域换题时，点「**重新截图提问**」即可，无需重新框。

### 快捷键（可自定义）
- **截图快捷键**（默认 `Alt+S`）：进入拖拽框选并自动截图提问
- **重新截图快捷键**（默认 `Alt+R`）：按上次框选区域直接重截提问

> 在设置里点对应输入框，直接按下你想要的组合键即可录制；按 `Esc`/`Backspace` 清空即关闭。

## 四、开发者：自行构建

```bash
git clone https://github.com/DUNHKpcc/ocs-ai-.git
cd ocs-ai-
pnpm install
VITE_BUILD_PATH=../../dist npm run build   # 产物：dist/ocs.ai.user.js
```

## 说明
- 截图通过浏览器 `getDisplayMedia` 真实捕获，仅存在于内存、不落地，刷新即清；图片会上传到你配置的 AI 接口。
- 本项目基于 [ocsjs](https://github.com/ocsjs/ocsjs) 精简改造，仅保留 AI 答题功能。
